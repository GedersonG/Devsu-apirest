spring:
  datasource:
    url: jdbc:mysql://localhost:3306/basedatos
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

appDescription: "Manejo de cuentas, clientes, movimientos y reportes by Devsu"
appVersion: "1.0.0"